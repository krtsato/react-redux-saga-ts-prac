version: "3.7"

services:
  frontend:
    build:
      context: .
      dockerfile:
    image: rrstp-img:latest
    ports:
      -  8080:8080
    volumes: 
    command: sh -c "cd front-container && npm start"
