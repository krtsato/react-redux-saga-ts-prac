version: "3.7"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: rrstp-img:latest
    ports:
      - 8000:8000 # webpack-dev-server
      - 8001:8001 # storybook
    volumes:
      - .:/container
    container_name: rrstp-ctr
    tty: true
