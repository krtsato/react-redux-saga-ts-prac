{
  "presets": [
    [
      "@babel/preset-env",
      {
        "targets": [">5% in JP", "not ie < 11", "not op_mini all"]
      }
    ],
    "@babel/preset-react",
    "@babel/preset-typescript"
  ]
}
